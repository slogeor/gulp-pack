'use strict';

module.exports = function (gulp, gulpConfig, plugins) {

    // gulp.task('templates', function () {
    //     var common = gulpConfig.common;

    //     return gulp.src([common.buildPagesFile])
    //         .pipe(replace(/foo(.{3})/g, '$1foo'))
    //         .pipe(gulp.dest('build/file.txt'));
    // });
};
